use umm::python::{new_project, new_query_grader, show_results};

pub async fn main() {
    let project = new_project()?;
    let grade = new_query_grader()
        .project(project)
        .file("hello")
        .req_name("test")
        .out_of(5.0)
        .must_not_use_for_loop()
        .run()
        .await?;
    show_results([grade])?;
    Ok(())
}
